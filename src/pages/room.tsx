import React from 'react';
import { Button } from 'antd';

const Room: React.FC = () => {
    return (
        <div>
            <Button type="primary">테스트 버튼</Button>
        </div>
    )
}

export default Room;